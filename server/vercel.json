{
  "version": 2,
  "installCommand": "npm install --include=dev --ignore-scripts && npm run postinstall",
  "buildCommand": "tsc",
  "builds": [
    { "src": "src/index.ts", "use": "@vercel/node@4" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" }
  ]
}