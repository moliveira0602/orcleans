{
  "version": 2,
  "installCommand": "npm ci --include=dev",
  "buildCommand": "npx prisma generate && npm run build",
  "builds": [
    { "src": "src/index.ts", "use": "@vercel/node@4" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" }
  ]
}