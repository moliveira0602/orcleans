{
  "version": 2,
  "builds": [
    { "src": "src/index.ts", "use": "@vercel/node", "config": { "installCommand": "cd ../server && npm install" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/index.ts" }
  ]
}